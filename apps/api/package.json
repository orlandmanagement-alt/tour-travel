{
  "name": "nusantaratrip-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
	"build": "echo 'No bundle step needed'",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute nusantaratrip_db --local --file=./src/db/schema.sql",
    "db:seed": "wrangler d1 execute nusantaratrip_db --local --file=./src/db/seed.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240329.0",
    "typescript": "^5.0.4",
    "wrangler": "^4.78.0"
  }
}
